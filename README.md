# How to use
1. Download `cipher.py`.
2. Open your terminal, and then navigate to the folder where `cipher.py` file is located.
3. Run `python cipher.py`. Follow the prompts to encrypt or decrypt messages.
